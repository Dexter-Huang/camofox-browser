# 该 sidecar 与 backend 虚拟环境隔离，版本必须与预置 Camoufox 兼容性 PoC 一并验证。
camoufox==0.5.5
# Python Playwright does not publish Node's 1.59.1 patch release; 1.59.0 is the
# nearest published Python driver and remains inside Camoufox 0.5.5's <1.61 bound.
playwright==1.59.0
fastapi==0.115.14
uvicorn[standard]==0.35.0
